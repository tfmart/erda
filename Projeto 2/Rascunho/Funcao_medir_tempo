#include <stdio.h>
#include <time.h>

int time(void)
{
    gettimeofday(&t0, 0);

    //code

    gettimeofday(&t1, 0);
    long long elapsed = (t1.tv_sec-t0.tv_sec)*1000000LL + t1.tv_usec-t0.tv_usec;
}

Utilizar no item OPCIONAL 2 do projeto, onde precisa mostrar o tempo de busca.
